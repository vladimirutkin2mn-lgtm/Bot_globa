.env
.venv/
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/
.ruff_cache/
.DS_Store

# JetBrains IDE workspace — personal, not shared.
.idea/

# Personal Claude Code permission allowlist — machine-local, not shared.
.claude/settings.local.json

# oh-my-claudecode working state (plans, notepad, logs, sessions).
.omc/

# Operator notes that name real hosts and reach real credentials. Never committed:
# a repository is cloned, and anything inside it leaves with every copy.
*.local.md
